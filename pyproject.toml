[project]
name = "mcp-calculator-server"
version = "0.1.0"
description = "Minimal MCP server with a single calculator tool (Model Context Protocol)."
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "mcp[cli]>=1.27.0",
]
